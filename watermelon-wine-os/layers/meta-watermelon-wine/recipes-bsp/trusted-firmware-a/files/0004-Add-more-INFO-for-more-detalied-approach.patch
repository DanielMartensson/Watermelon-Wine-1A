From 091ea9ed2e3f4b10d1bbb44dbf02b782ccbb75c7 Mon Sep 17 00:00:00 2001
From: noname <noreply@example.com>
Date: Fri, 15 May 2026 14:08:19 +0200
Subject: [PATCH 4/4] Add more INFO for more detalied approach

---
 lib/el3_runtime/aarch64/context_mgmt.c | 27 ++++++++++++--------------
 plat/st/stm32mp2/bl31_plat_setup.c     |  1 +
 2 files changed, 13 insertions(+), 15 deletions(-)

diff --git a/lib/el3_runtime/aarch64/context_mgmt.c b/lib/el3_runtime/aarch64/context_mgmt.c
index d2fdfddb8..11bf93406 100644
--- a/lib/el3_runtime/aarch64/context_mgmt.c
+++ b/lib/el3_runtime/aarch64/context_mgmt.c
@@ -114,7 +114,7 @@ static void setup_secure_context(cpu_context_t *ctx, const struct entry_point_in
 {
 	u_register_t scr_el3;
 	el3_state_t *state;
-
+	
 	state = get_el3state_ctx(ctx);
 	scr_el3 = read_ctx_reg(state, CTX_SCR_EL3);
 
@@ -142,7 +142,7 @@ static void setup_secure_context(cpu_context_t *ctx, const struct entry_point_in
 		scr_el3 |= SCR_ATA_BIT;
 	}
 #endif /* CTX_INCLUDE_MTE_REGS */
-
+	
 	write_ctx_reg(state, CTX_SCR_EL3, scr_el3);
 
 	/*
@@ -151,8 +151,8 @@ static void setup_secure_context(cpu_context_t *ctx, const struct entry_point_in
 	 */
 #if !SPMD_SPM_AT_SEL2
 	setup_el1_context(ctx, ep);
-#endif
-
+#endif	
+	
 	manage_extensions_secure(ctx);
 
 	/**
@@ -161,7 +161,7 @@ static void setup_secure_context(cpu_context_t *ctx, const struct entry_point_in
 	 * all the cpus for the secure world.
 	 * Henceforth, this check ensures that the registers are initialised once
 	 * and avoids re-initialization from multiple cores.
-	 */
+	 */ 
 	if (!has_secure_perworld_init) {
 		manage_extensions_secure_per_world();
 	}
@@ -177,17 +177,17 @@ static void setup_realm_context(cpu_context_t *ctx, const struct entry_point_inf
 {
 	u_register_t scr_el3;
 	el3_state_t *state;
-
+	
 	state = get_el3state_ctx(ctx);
 	scr_el3 = read_ctx_reg(state, CTX_SCR_EL3);
-
+	
 	scr_el3 |= SCR_NS_BIT | SCR_NSE_BIT;
-
+	
 	if (is_feat_csv2_2_supported()) {
 		/* Enable access to the SCXTNUM_ELx registers. */
 		scr_el3 |= SCR_EnSCXT_BIT;
 	}
-
+	
 	write_ctx_reg(state, CTX_SCR_EL3, scr_el3);
 }
 #endif /* ENABLE_RME */
@@ -200,7 +200,6 @@ static void setup_ns_context(cpu_context_t *ctx, const struct entry_point_info *
 {
 	u_register_t scr_el3;
 	el3_state_t *state;
-
 	state = get_el3state_ctx(ctx);
 	scr_el3 = read_ctx_reg(state, CTX_SCR_EL3);
 
@@ -250,7 +249,6 @@ static void setup_ns_context(cpu_context_t *ctx, const struct entry_point_info *
 	 */
 	scr_el3 |= SCR_TERR_BIT;
 #endif
-
 	if (is_feat_csv2_2_supported()) {
 		/* Enable access to the SCXTNUM_ELx registers. */
 		scr_el3 |= SCR_EnSCXT_BIT;
@@ -279,7 +277,7 @@ static void setup_ns_context(cpu_context_t *ctx, const struct entry_point_info *
 	sctlr_el2 |= SCTLR_EL2_RES1;
 	write_ctx_reg(get_el2_sysregs_ctx(ctx), CTX_SCTLR_EL2,
 			sctlr_el2);
-
+	
 	if (is_feat_hcx_supported()) {
 		/*
 		 * Initialize register HCRX_EL2 with its init value.
@@ -292,7 +290,7 @@ static void setup_ns_context(cpu_context_t *ctx, const struct entry_point_info *
 		write_ctx_reg(get_el2_sysregs_ctx(ctx), CTX_HCRX_EL2,
 			HCRX_EL2_INIT_VAL);
 	}
-
+	
 	if (is_feat_fgt_supported()) {
 		/*
 		 * Initialize HFG*_EL2 registers with a default value so legacy
@@ -307,7 +305,6 @@ static void setup_ns_context(cpu_context_t *ctx, const struct entry_point_info *
 			HFGWTR_EL2_INIT_VAL);
 	}
 #endif /* CTX_INCLUDE_EL2_REGS */
-
 	manage_extensions_nonsecure(ctx);
 }
 
@@ -659,7 +656,7 @@ static void manage_extensions_secure_per_world(void)
 		/*
 		 * Enable SVE and FPU in secure context, SPM must ensure
 		 * that the SVE and FPU register contexts are properly managed.
-		 */
+		 */	
 			sve_enable_per_world(&per_world_context[CPU_CONTEXT_SECURE]);
 		} else {
 		/*
diff --git a/plat/st/stm32mp2/bl31_plat_setup.c b/plat/st/stm32mp2/bl31_plat_setup.c
index b96f60ac3..657d83c10 100644
--- a/plat/st/stm32mp2/bl31_plat_setup.c
+++ b/plat/st/stm32mp2/bl31_plat_setup.c
@@ -185,6 +185,7 @@ void bl31_plat_runtime_setup(void)
 	}
 	INFO("Entering console_switch_state(CONSOLE_FLAG_RUNTIME)\n");
 	console_switch_state(CONSOLE_FLAG_RUNTIME);
+	INFO("Exit bl31_plat_runtime_setup(void)\n");
 }
 
 entry_point_info_t *bl31_plat_get_next_image_ep_info(unsigned int type)
-- 
2.43.0

